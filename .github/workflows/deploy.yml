name: Deploy Render

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Trigger Render Deploy
        run: |
          echo "Deploy será feito automaticamente pelo Render via webhook"
          echo "Commit: ${{ github.sha }}"